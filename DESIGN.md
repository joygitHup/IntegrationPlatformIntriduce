# DESIGN.md

## 项目
集成中台 (IntegrationPlatform) 官网落地页优化

## 设计优化方向

### 气质与意象
- **基调**：深色科技风，以「数据管道」为核心意象——数据像水流一样在系统间有序流动
- **场景感**：不是冰冷的技术仪表盘，而是「精密的现代数据枢纽」——有序、可靠、可控
- **关键词**：流动感、精密感、信任感、专业感

### 视觉策略优化
- **层次强化**：原版卡片同质化严重，新增「主视觉梯度」——首屏 Hero 更有冲击力，中部信息密度高但有节奏，CTA 有力量感
- **图形语言**：新增更多 SVG 微动画（数据粒子、发光管道），用 CSS 原生实现，不依赖外部库
- **空间感**：通过多层级玻璃拟态 + 模糊背景 + 景深光斑，增强页面深度

### 配色方案
- **主色**：`#3b82f6`（科技蓝）→ 优化为 `#2563eb → #0ea5e9` 渐变，更沉稳
- **辅助色**：`#06b6d4`（青）、`#8b5cf6`（紫）、`#10b981`（绿）、`#f59e0b`（琥珀）、`#f43f5e`（玫红）
- **背景色阶**：`#0b1220 → #0f172a → #1e293b` 三层递进
- **新增**：成功绿 `#10b981`、告警橙 `#f59e0b`、失败红 `#ef4444` 状态色系统

### 字体排版
- **字体**：Inter + PingFang SC + Microsoft YaHei（保持原版，CN 域加载）
- **排版节奏**：标题使用 1.1 行高、大字号、紧字距；正文 1.6 行高、舒适字距
- **新增**：渐变文字用于核心卖点，增强视觉焦点

### 动效与交互优化
1. **导航**：滚动时背景模糊 + 轻微缩小，增强沉浸感
2. **Hero**：数据粒子背景动画（CSS 实现），管道流动更丝滑
3. **卡片悬停**：上移 + 发光边框 + 内阴影，提升反馈感
4. **数字滚动**：用 IntersectionObserver 触发数字计数动画
5. **FAQ 展开**：平滑高度过渡，而非突兀展开
6. **连接器卡片**：悬停时图标轻微缩放，分类色彩更鲜明
7. **定价切换**：价格数字有淡入+数字滚动过渡
8. **回到顶部**：滚动超过一屏后出现

### 性能优化
- **字体**：使用 Google Fonts CN 域名 + font-display: swap
- **动画**：优先使用 transform/opacity，避免重排
- **资源**：去掉 AOS 库，用原生 IntersectionObserver 实现滚动动画（减少依赖）
- **图片**：使用 SVG 矢量图形，无位图依赖

### 可访问性
- 所有交互元素有 focus 状态
- 语义化 HTML 结构
- 颜色对比度达标

### 设计禁忌
- ❌ 避免大面积高饱和渐变（刺眼）
- ❌ 避免过多 emoji（不够专业）
- ❌ 避免长段落无分组（信息密度不均）
- ❌ 避免所有卡片看起来一样（缺乏视觉层级）
